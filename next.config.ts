import type { NextConfig } from "next";

/**
 * Configuración base de Next.js.
 *
 * Se mantiene mínima porque la app usa el App Router nativo y no requiere
 * reglas especiales de build por ahora.
 */
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
