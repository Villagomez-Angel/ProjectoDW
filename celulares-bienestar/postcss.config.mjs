/**
 * Configuración de PostCSS.
 *
 * En este proyecto sólo se necesita el plugin oficial de Tailwind CSS.
 */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
